<div class="reg_header">
    {% if app.user and studyId is not defined %}
    	<a href="{{ path('_main') }}" class="logo">ProofPilot</a>
    {% else  %}
        {{ study_logo(studyId)|raw}}
    {%  endif %}
</div>